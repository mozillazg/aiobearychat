.. highlight:: shell

============
Installation
============

At the command line::

    $ pip install aiobearychat

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv aiobearychat
    $ pip install aiobearychat
